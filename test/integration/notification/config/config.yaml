database:
  host: host.containers.internal
  port: 15440  # Changed from 15439 to resolve conflict with HAPI (DD-TEST-001 v2.0)
  user: slm_user
  name: action_history
  sslmode: disable
  secretsFile: /etc/datastorage/db-secrets.yaml
  usernameKey: username
  passwordKey: password

redis:
  addr: host.containers.internal:16385
  secretsFile: /etc/datastorage/redis-secrets.yaml
  passwordKey: password

immudb:
  host: notification_immudb_test
  port: 3322
  database: kubernaut_audit
  username: immudb
  tls_enabled: false
  secretsFile: /etc/datastorage/secrets/immudb-secrets.yaml
  passwordKey: password

server:
  port: 8080
  metrics_port: 9090
