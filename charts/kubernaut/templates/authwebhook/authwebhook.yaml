---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: authwebhook
  namespace: {{ .Release.Namespace }}
  labels:
    app: authwebhook
    {{- include "kubernaut.labels" . | nindent 4 }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: authwebhook
  labels:
    {{- include "kubernaut.labels" . | nindent 4 }}
rules:
  - apiGroups: ["kubernaut.ai"]
    resources: ["workflowexecutions", "remediationapprovalrequests", "notificationrequests", "remediationrequests", "actiontypes"]
    verbs: ["get", "list", "watch"]
  # Issue #418: RW reconciler needs update/patch to add/remove the catalog-cleanup finalizer
  - apiGroups: ["kubernaut.ai"]
    resources: ["remediationworkflows"]
    verbs: ["get", "list", "watch", "update", "patch"]
  - apiGroups: ["kubernaut.ai"]
    resources: ["remediationworkflows/finalizers"]
    verbs: ["update"]
  - apiGroups: ["kubernaut.ai"]
    resources: ["workflowexecutions/status", "remediationapprovalrequests/status", "remediationrequests/status", "remediationworkflows/status", "actiontypes/status"]
    verbs: ["update", "patch"]
  {{- if eq .Values.tls.mode "hook" }}
  - apiGroups: ["admissionregistration.k8s.io"]
    resources: ["mutatingwebhookconfigurations", "validatingwebhookconfigurations"]
    verbs: ["get", "patch"]
  {{- end }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: authwebhook
  labels:
    {{- include "kubernaut.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: authwebhook
subjects:
  - kind: ServiceAccount
    name: authwebhook
    namespace: {{ .Release.Namespace }}
---
apiVersion: v1
kind: Service
metadata:
  name: authwebhook
  namespace: {{ .Release.Namespace }}
  labels:
    app: authwebhook
    {{- include "kubernaut.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: authwebhook
  ports:
    - name: https
      port: 443
      targetPort: webhook
      protocol: TCP
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: authwebhook-config
  namespace: {{ .Release.Namespace }}
  labels:
    app: authwebhook
    {{- include "kubernaut.labels" . | nindent 4 }}
data:
  authwebhook.yaml: |
    logging:
      level: {{ .Values.authwebhook.logging.level | default "INFO" | quote }}
    webhook:
      port: 9443
      certDir: /tmp/k8s-webhook-server/serving-certs
      healthProbeAddr: ":8081"
    datastorage:
      url: "{{ include "kubernaut.datastorage.url" . }}"
      timeout: 30s
      buffer:
        bufferSize: 1000
        batchSize: 100
        flushInterval: 5s
        maxRetries: 3
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: authwebhook
  namespace: {{ .Release.Namespace }}
  labels:
    app: authwebhook
    {{- include "kubernaut.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.authwebhook.replicas }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: authwebhook
  template:
    metadata:
      labels:
        app: authwebhook
    spec:
      serviceAccountName: authwebhook
      {{- include "kubernaut.imagePullSecrets" . | nindent 6 }}
      securityContext:
        {{- include "kubernaut.podSecurityContext" .Values.authwebhook | nindent 8 }}
      {{- include "kubernaut.affinity" .Values.authwebhook | nindent 6 }}
      {{- include "kubernaut.scheduling" (dict "component" .Values.authwebhook "global" .Values.global) | nindent 6 }}
      {{- if eq .Values.tls.mode "hook" }}
      initContainers:
        - name: patch-cabundle
          image: {{ .Values.hooks.tlsCerts.image }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          securityContext:
            {{- include "kubernaut.containerSecurityContext" .Values.hooks | nindent 12 }}
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
          volumeMounts:
            - name: webhook-certs
              mountPath: /certs
              readOnly: true
          command:
            - /bin/sh
            - -c
            - |
              set -e
              CA_FILE="/certs/ca.crt"
              if [ ! -f "$CA_FILE" ]; then
                echo "WARNING: $CA_FILE not found — skipping caBundle patch"
                exit 0
              fi

              CA_BUNDLE=$(base64 < "$CA_FILE" | tr -d '\n')

              # Wait for the API server to become reachable before patching.
              # Kind clusters under heavy Service/Endpoint churn (repeated
              # helm install/upgrade/uninstall cycles in CI smoke tests) can
              # see transient iptables/conntrack sync delays that make the
              # in-cluster kubernetes.default ClusterIP briefly unreachable
              # ("dial tcp 10.96.0.1:443: i/o timeout"). Retrying here in
              # seconds is far cheaper than relying on kubelet's
              # container-restart backoff, which escalates into
              # multi-minute waits for the same transient condition.
              #
              # --request-timeout is mandatory on every call below: without
              # it, kubectl's discovery client (client-go memcache.go) issues
              # its own internal API-group-list request with a hardcoded 32s
              # timeout and retries it several times before the command
              # actually returns — confirmed via CI must-gather logs showing
              # "Unhandled Error" retries at exact 30s intervals, so a single
              # unbounded kubectl invocation could block for 2.5+ minutes
              # instead of failing fast, starving this retry loop of attempts
              # (ref: kubectl/client-go discovery timeout defaults; see also
              # https://cloud.google.com/kubernetes-engine/distributed-cloud/vmware/docs/troubleshooting/kube-apiserver).
              wait_for_api_server() {
                ATTEMPTS=20
                i=0
                while [ "$i" -lt "$ATTEMPTS" ]; do
                  if kubectl get --raw=/healthz --request-timeout=3s >/dev/null 2>&1; then
                    return 0
                  fi
                  i=$((i + 1))
                  sleep 2
                done
                echo "ERROR: API server unreachable after ${ATTEMPTS} attempts (~100s)"
                return 1
              }

              patch_webhooks() {
                KIND="$1"
                NAME="$2"
                COUNT=$(kubectl get "$KIND" "$NAME" --request-timeout=5s -o jsonpath='{.webhooks[*].name}' | wc -w | tr -d ' ')
                PATCH="["
                i=0
                while [ "$i" -lt "$COUNT" ]; do
                  [ "$i" -gt 0 ] && PATCH="${PATCH},"
                  PATCH="${PATCH}{\"op\":\"add\",\"path\":\"/webhooks/${i}/clientConfig/caBundle\",\"value\":\"${CA_BUNDLE}\"}"
                  i=$((i + 1))
                done
                PATCH="${PATCH}]"
                echo "  Patching $NAME ($COUNT webhooks)..."
                kubectl patch "$KIND" "$NAME" --request-timeout=5s --type='json' -p "$PATCH"
              }

              echo "==> Init-container: patching webhook caBundle (#377 self-healing)..."
              wait_for_api_server
              patch_webhooks mutatingwebhookconfigurations authwebhook-mutating
              patch_webhooks validatingwebhookconfigurations authwebhook-validating
              echo "==> Webhook caBundle patching complete"
      {{- end }}
      containers:
        - name: authwebhook
          image: {{ include "kubernaut.image" (dict "service" "authwebhook" "global" .Values.global "appVersion" .Chart.AppVersion) }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          securityContext:
            {{- include "kubernaut.containerSecurityContext" .Values.authwebhook | nindent 12 }}
          args:
            - -config=/etc/authwebhook/authwebhook.yaml
          ports:
            - name: webhook
              containerPort: 9443
              protocol: TCP
            - name: health
              containerPort: 8081
              protocol: TCP
          env:
            - name: LOG_LEVEL
              value: "debug"
            - name: TLS_CA_FILE
              value: {{ include "kubernaut.interServiceTLS.caFile" . | quote }}
          volumeMounts:
            - name: webhook-certs
              mountPath: /tmp/k8s-webhook-server/serving-certs
              readOnly: true
            - name: config
              mountPath: /etc/authwebhook
              readOnly: true
            - name: tls-ca
              mountPath: /etc/tls-ca
              readOnly: true
          resources:
            {{- toYaml .Values.authwebhook.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
              scheme: HTTP
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
              scheme: HTTP
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
      volumes:
        - name: webhook-certs
          secret:
            secretName: authwebhook-tls
        - name: config
          configMap:
            name: authwebhook-config
        - name: tls-ca
          configMap:
            name: inter-service-ca
            optional: true
