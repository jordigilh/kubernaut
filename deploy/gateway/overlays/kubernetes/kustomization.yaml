apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: gateway-kubernetes
  annotations:
    description: "Gateway Service for vanilla Kubernetes - uses base configuration as-is"

resources:
  - ../../base

# Vanilla Kubernetes uses base configuration without modifications
# securityContext with runAsUser and fsGroup is preserved
