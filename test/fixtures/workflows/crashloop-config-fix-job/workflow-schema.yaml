apiVersion: kubernaut.ai/v1alpha1
kind: RemediationWorkflow
metadata:
  name: crashloop-config-fix-v1
spec:
  # CrashLoop Config Fix Workflow Schema - Job Backend (BR-WORKFLOW-004)
  # Used by: FullPipeline E2E (execution.engine: job)
  version: "1.0.0"
  description:
    what: "Restarts deployment to recover from CrashLoopBackOff caused by configuration issues"
    whenToUse: "When pods are in CrashLoopBackOff due to misconfiguration"
    whenNotToUse: "When crash is caused by a persistent code bug"
    preconditions: "Pod is managed by a Deployment"
  
  actionType: RestartDeployment
  
  labels:
    severity: [high, warning]
    environment: [production, staging, test]
    component: ["*"]
    priority: "*"
  
  execution:
    engine: job
    # Issue #1650: pinned to an immutable "legacy-v1.5.2-exec" tag (not the
    # shared/mutable "v1.0.0-exec" tag main uses) because main's Issue #1542
    # rebuilt that tag with a real-remediation script requiring different
    # parameters (CONFIGMAP_NAME/KEY/VALUE) than this release/v1.5 branch's
    # mock LLM injects (NAMESPACE/DEPLOYMENT_NAME). This tag+digest preserves
    # the v1.5.2-era simulation script this branch's test infra expects.
    bundle: quay.io/kubernaut-cicd/test-workflows/crashloop-config-fix-job:legacy-v1.5.2-exec@sha256:449cec99ee184a2c9071fcfec6c43c861d963b3f431c8af81b4cbbc8bd8037a7
  
  parameters:
    - name: TARGET_RESOURCE_NAME
      type: string
      required: true
      description: "Name of the root managing resource (KA-injected)"
    - name: TARGET_RESOURCE_KIND
      type: string
      required: true
      description: "Kind of the root managing resource (KA-injected)"
    - name: TARGET_RESOURCE_NAMESPACE
      type: string
      required: true
      description: "Namespace of the root managing resource (KA-injected)"
    - name: NAMESPACE
      type: string
      required: true
      description: "Target namespace"
    - name: DEPLOYMENT_NAME
      type: string
      required: true
      description: "Name of the deployment to restart"
    - name: GRACE_PERIOD_SECONDS
      type: integer
      required: false
      description: "Graceful shutdown period in seconds"
      default: 30
      minimum: 0
      maximum: 300
