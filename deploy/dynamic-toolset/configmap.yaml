apiVersion: v1
kind: ConfigMap
metadata:
  name: dynamic-toolset-config
  namespace: kubernaut-system
  labels:
    app: dynamic-toolset
    component: toolset-discovery
data:
  # Discovery configuration
  discovery-interval: "5m"
  namespaces: "monitoring,observability,default"

  # Optional: Service detection configuration
  # detector-config.yaml: |
  #   detectors:
  #     - type: prometheus
  #       enabled: true
  #       labels:
  #         app: prometheus
  #       healthCheck:
  #         enabled: true
  #         path: /-/healthy
  #     - type: grafana
  #       enabled: true
  #       labels:
  #         app: grafana
  #       healthCheck:
  #         enabled: true
  #         path: /api/health

