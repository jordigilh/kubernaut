{{- /*
Workflow Discovery Context Template (DetectedLabels Filter Instructions)

Ported from: holmesgpt-api/src/extensions/incident/prompt_builder.py (build_mcp_filter_instructions)
Design Decisions: DD-HAPI-001, DD-WORKFLOW-001 v2.1

Builds the DetectedLabels filter instructions appended to the prompt so the LLM
understands which detected environment characteristics are active and how to use
them for workflow selection.

Data fields:
  .FiltersJSON    string  - Pre-serialized JSON of the filter map (built by Go code)
  .GuidanceText   string  - Pre-built conditional guidance lines (built by Go code)
  .HasFilters     bool    - True if at least one filter is present
*/ -}}
{{- if not .HasFilters -}}
{{- else }}
### Workflow Discovery Context (DetectedLabels)

The following detected labels describe the target cluster environment.
Use these to inform your workflow selection reasoning — prefer workflows
that are compatible with the detected environment characteristics.

Detected environment characteristics:
```json
{{ .FiltersJSON }}
```

**IMPORTANT**: {{ .GuidanceText }}
{{- end -}}
