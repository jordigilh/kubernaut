# Gateway Service Configuration - Test Fixture
# BR-GATEWAY-024: Configuration management
# DD-GATEWAY-012: Redis REMOVED - Gateway is now Redis-free

server:
  listen_addr: ":8080"
  read_timeout: 30s
  write_timeout: 30s
  idle_timeout: 120s

middleware:
  # Rate limiting removed (ADR-048) - delegated to proxy

infrastructure:
  # DD-GATEWAY-012: Redis REMOVED - Gateway uses K8s status for state
  # DD-AUDIT-003: Data Storage URL for audit event emission
  data_storage_url: "http://data-storage-service:8080"

processing:
  deduplication:
    ttl: 5m
  storm:
    rate_threshold: 10
    pattern_threshold: 5
    # DD-GATEWAY-008: Storm aggregation settings
    buffer_threshold: 5
    inactivity_timeout: 60s
    max_window_duration: 5m
  # environment settings removed (DD-CATEGORIZATION-001)
