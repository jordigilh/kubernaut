server:
  port: 8091
  host: "0.0.0.0"
  readTimeout: "30s"
  writeTimeout: "30s"

logging:
  level: "info"
  format: "json"

cache:
  redis_addr: "localhost:6379"
  redis_db: 0
  lru_size: 1000
  default_ttl: "5m"

database:
  host: "localhost"
  port: 5432
  name: "action_history"
  user: "slm_user"
  password: "slm_password_dev"
  sslMode: "disable"


