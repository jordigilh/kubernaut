# Malformed YAML file for testing error handling

server:
  listenAddr: ":8080"
  readTimeout: "30s"
  # Missing closing quote
  writeTimeout: "30s
  idleTimeout: "120s"

# Invalid indentation
middleware:
  # Rate limiting removed (ADR-048)


