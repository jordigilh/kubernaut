# This rule is not used by the project kubernaut itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to workflowexecution.kubernaut.io resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kubernaut
    app.kubernetes.io/managed-by: kustomize
  name: workflowexecution-workflowexecution-viewer-role
rules:
- apiGroups:
  - workflowexecution.kubernaut.io
  resources:
  - workflowexecutions
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - workflowexecution.kubernaut.io
  resources:
  - workflowexecutions/status
  verbs:
  - get
