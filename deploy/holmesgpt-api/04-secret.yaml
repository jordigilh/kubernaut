---
apiVersion: v1
kind: Secret
metadata:
  name: holmesgpt-api-secret
  namespace: kubernaut-ai
  labels:
    app.kubernetes.io/name: holmesgpt-api
    app.kubernetes.io/component: ai-analysis
    app.kubernetes.io/part-of: kubernaut
type: Opaque
stringData:
  # LLM credentials (replace with actual values)
  LLM_API_KEY: "REPLACE_WITH_YOUR_LLM_API_KEY"
  LLM_MODEL: "REPLACE_WITH_YOUR_MODEL_NAME"  # e.g., "gpt-4", "claude-3-opus"
  LLM_ENDPOINT: ""  # Optional: custom LLM endpoint

  # Context API URL (internal service)
  CONTEXT_API_URL: "http://context-api.kubernaut-system.svc.cluster.local:8091"



