# Malformed YAML - Test Fixture for error handling
# BR-GATEWAY-024: Configuration validation

server:
  listenAddr: ":8080"
  readTimeout: 30s
  invalid_yaml_structure_here
    this: is: not: valid: yaml:
  - missing
  - proper
  - indentation
middleware:
  rate_limit
    requests_per_minute 100

