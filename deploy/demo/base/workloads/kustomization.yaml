apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  # Choose ONE memory-eater variant to deploy:
  # - OOMKill: triggers K8s OOMKill events -> Event Exporter -> Gateway
  # - HighUsage: triggers Prometheus alert -> AlertManager -> Gateway
  #
  # Default: OOMKill variant (faster trigger, simpler demo flow)
  - memory-eater-oomkill.yaml
  # Uncomment below and comment above for Prometheus alert demo:
  # - memory-eater-high-usage.yaml
