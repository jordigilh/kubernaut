apiVersion: kubernaut.ai/v1alpha1
kind: RemediationWorkflow
metadata:
  name: generic-restart-v1
spec:
  # Generic Restart Workflow Schema (BR-WORKFLOW-004)
  # Used by: AIAnalysis E2E, HolmesGPT-API E2E
  version: "1.0.0"
  description:
    what: "Restarts a pod as a generic recovery action"
    whenToUse: "When a pod needs a restart and no specific remediation applies"
    whenNotToUse: "When root cause is known and a targeted fix exists"
    preconditions: "Pod is managed by a controller"
  
  actionType: RestartPod
  
  labels:
    severity: [low, medium, high]
    environment: [production, staging, test]
    component: pod
    priority: P2
  
  execution:
    engine: tekton
    bundle: quay.io/kubernaut-cicd/test-workflows/placeholder-execution:v1.0.0@sha256:2a07b572d7c2d16845da11ea01ff24198bd2ff366580c81a8d2fbc6ae6b78389
  
  parameters:
    - name: TARGET_RESOURCE_NAME
      type: string
      required: true
      description: "Name of the root managing resource (HAPI-injected)"
    - name: TARGET_RESOURCE_KIND
      type: string
      required: true
      description: "Kind of the root managing resource (HAPI-injected)"
    - name: TARGET_RESOURCE_NAMESPACE
      type: string
      required: true
      description: "Namespace of the root managing resource (HAPI-injected)"
    - name: NAMESPACE
      type: string
      required: true
      description: "Target namespace"
    - name: POD_NAME
      type: string
      required: true
      description: "Name of the pod to restart"
