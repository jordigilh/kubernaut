---
globs: "*_test.go,test/**/*"
description: "Testing strategy, anti-patterns, interface validation, and coverage standards for kubernaut"
---

> **Policy Authority**: [AGENTS.md](../../AGENTS.md) defines all mandatory methodology,
> including TDD workflow, Pyramid Invariant, coverage thresholds (>=80% per tier),
> mock strategy policy, and testing anti-patterns. This file provides Cursor-specific
> implementation patterns and code examples.

# Testing Patterns for Kubernaut

## CRITICAL: KUBERNETES CLIENT MANDATE

**AUTHORITATIVE RULE (ADR-004)**: All services that interact with Kubernetes MUST use the approved K8s client interface:

| Test Tier | MANDATORY Interface | Package |
|-----------|---------------------|---------|
| **Unit Tests** | Fake K8s Client | `sigs.k8s.io/controller-runtime/pkg/client/fake` |
| **Integration Tests** | Real K8s API (envtest/KIND) | `sigs.k8s.io/controller-runtime/pkg/client` |
| **E2E Tests** | Real K8s API (OCP/KIND) | `sigs.k8s.io/controller-runtime/pkg/client` |

**FORBIDDEN**: Custom `MockK8sClient` implementations -- use fake client instead.
**APPROVED**: `fake.NewClientBuilder()` for all unit tests requiring K8s interaction.

**See**: [ADR-004: Fake Kubernetes Client for Unit Testing](docs/architecture/decisions/ADR-004-fake-kubernetes-client.md)

---

## Test Tier Patterns

### Unit Tests

**Location**: Colocated with production code in `pkg/` and `internal/` (black-box `package <name>_test`).
**Execution**: `make test-unit-<service>` or `go test ./pkg/<service>/...`
**Framework**: Ginkgo/Gomega BDD (MANDATORY)

**Mock Strategy**:
```go
// Mock ONLY external dependencies
mockLLMProvider   *mocks.MockLLMProvider    // External: AI service
mockK8sClient     client.Client              // External: fake.NewClientBuilder()
mockVectorDB      *mocks.MockVectorDatabase  // External: Database

// Use REAL business logic components
workflowBuilder   *engine.IntelligentWorkflowBuilder
safetyFramework   *platform.SafetyFramework
```

### Integration Tests

**Location**: `test/integration/`
**Execution**: `make test-integration-[service]`
**Framework**: Ginkgo/Gomega BDD with envtest/KIND

Zero mocks for business logic. Real K8s API via envtest. Real databases via testcontainers. Mock ONLY external services (LLM, external APIs).

### E2E Tests

**Location**: `test/e2e/`
**Execution**: `make test-e2e-[service]`
**Framework**: Ginkgo/Gomega BDD with KIND/OCP clusters

Zero mocks for internal components. Real Kubernetes clusters. Mock LLM MAY be acceptable for test speed.

---

## Mock Reuse Patterns

### Mock Decision Matrix

| Component Type | Action |
|---------------|--------|
| **External Services** (AI, K8s, DB) | Use existing mocks from `pkg/testutil/mocks/` |
| **Business Logic** (Engine, Analytics) | Use REAL components |
| **Configuration** | Use real config with test values |
| **Utilities** | Use real utilities |

**Red flag**: >3 mocks in a single test. Use real business components instead.

---

## Anti-Pattern Code Examples

### NULL-TESTING (Most Critical)

```go
// FORBIDDEN
Expect(result).ToNot(BeNil())
Expect(result).ToNot(BeEmpty())
Expect(len(items)).To(BeNumerically(">", 0))

// CORRECT: Business outcome validation
Expect(workflow.SafetyValidation).To(ContainElement("resource-limits"))
Expect(analysis.ConfidenceScore).To(BeNumerically(">=", 0.8))
```

### STATIC DATA TESTING

```go
// FORBIDDEN
input := "test-alert-name"
Expect(result.Name).To(Equal("test-alert-name"))

// CORRECT
input := testutil.GenerateAlertName()
Expect(result.Name).To(MatchRegexp("^alert-[0-9]+$"))
```

### LIBRARY TESTING

```go
// FORBIDDEN
logger := logrus.New()
Expect(logger).ToNot(BeNil())

// CORRECT
logger := business.NewStructuredLogger(config)
logger.LogWorkflowEvent("workflow-123", "completed")
Expect(logger.GetLastEvent().WorkflowID).To(Equal("workflow-123"))
```

### IMPLEMENTATION TESTING

```go
// FORBIDDEN
Expect(engine.parseWorkflowSteps(input)).To(HaveLen(3))
Expect(workflow.internalCache).To(HaveKey("step-1"))

// CORRECT
workflow, err := engine.CreateWorkflow(input)
Expect(err).ToNot(HaveOccurred())
Expect(workflow.Status).To(Equal(business.StatusReady))
Expect(workflow.CanExecute()).To(BeTrue())
```

### MOCK OVERUSE

```go
// FORBIDDEN: All business logic mocked = testing nothing
mockValidator := mocks.NewMockWorkflowValidator()
mockAnalyzer := mocks.NewMockPerformanceAnalyzer()
engine := NewEngine(mockValidator, mockAnalyzer)

// CORRECT: Real business logic, mock only external deps
validator := business.NewWorkflowValidator()
analyzer := business.NewPerformanceAnalyzer()
engine := NewEngine(validator, analyzer, mockLLMClient, mockK8sClient)
```

### ENVIRONMENT VARIABLE CONFIGURATION - Python (ADR-030)

```python
# FORBIDDEN
url = os.getenv("DATA_STORAGE_URL")
timeout = int(os.getenv("TIMEOUT", "30"))

# CORRECT: YAML configuration
import yaml
with open(config_file, 'r') as f:
    config = yaml.safe_load(f)
url = config['data_storage']['url']
```

**ONLY Exception**: `CONFIG_FILE` env var to specify YAML file location.

### Anti-Pattern Quick Reference

| Anti-Pattern | Detection | Fix |
|--------------|-----------|-----|
| NULL-TESTING | `BeNil()\|BeEmpty()` | Test business outcomes |
| STATIC DATA | Hardcoded strings | Use testutil generators |
| LIBRARY TESTING | Testing `logrus`, `context` | Test business logic |
| IMPLEMENTATION | Testing internal methods | Test public behavior |
| MOCK OVERUSE | >3 mocks | Use real business components |
| ENV VAR CONFIG (Python) | `os.getenv\|os.environ` | Use YAML config files |

---

## Interface and Method Validation

### Code Generation Halt Protocol

**BEFORE generating ANY line of test code:**

1. Search for existing real implementations, then existing mock implementations
2. If real business logic exists, PREFER it over mocks; if using mocks, use existing ones
3. Check for common build error patterns
4. Verify all imports exist and are properly used
5. If attempting to create duplicate mocks, IMMEDIATELY STOP and use existing

### Validation Sequence

**Step 1 - Interface Discovery**:
```bash
grep -r "type.*[InterfaceName].*interface" pkg/ --include="*.go"
```

**Step 2 - Method Signature Validation**:
```bash
grep -A 20 "type.*[InterfaceName].*interface" [interface_file.go]
```

**Step 3 - Mock Reuse Check**:
```bash
find pkg/testutil/mocks/ -name "*[ComponentName]*" -type f
grep -r "Mock[ComponentName]" pkg/testutil/ --include="*.go"
```

**Step 4 - Compilation Verification**:
```bash
go build ./test/[test_package]/ 2>&1 | tee build_check.log
grep "undefined:\|cannot use\|not enough arguments" build_check.log
```

**Step 5 - Import Consistency**:
```bash
go mod tidy && goimports -w [test_file.go]
go build [test_file.go] 2>&1 | grep "imported and not used"
```

---

## BDD Framework Patterns

```go
// Test identification: prefer Test Scenario ID, fallback to BR reference
Describe("UT-WF-197-001: Workflow Creation with Safety Validation", func() { ... })
Describe("BR-WORKFLOW-001: Intelligent Workflow Generation", func() { ... })

// FORBIDDEN: standard Go testing
func TestWorkflow(t *testing.T) { }

// CORRECT: Ginkgo/Gomega
var _ = Describe("Workflow Engine", func() {
    It("should process workflows", func() { ... })
})
```

---

## Automation Commands

```bash
# Run test pyramid
make test                          # Unit tests
make test-integration-[service]    # Integration tests
make test-e2e-[service]            # E2E tests

# Anti-pattern detection
make lint-test-patterns            # Detect NULL-TESTING, mock overuse
./scripts/validation/check-test-anti-patterns.sh --verbose

# Business integration verification
make lint-business-integration     # Check cmd/ integration
make lint-tdd-compliance           # Check BDD framework, BR references

# Coverage measurement
make coverage-report

# Python env-var detection (ADR-030)
grep -r "os.getenv" kubernaut-agent/ --include="*.py" | grep -v "CONFIG_FILE"
```

---

## Documentation References

- [AGENTS.md](../../AGENTS.md) - Mandatory methodology, TDD workflow, coverage policy
- [Testing Strategy Guide](docs/testing/README.md)
- [Wiring Verification](../../AGENTS.md#wiring-verification)
- [Testing Patterns Quick Reference](docs/testing/TESTING_PATTERNS_QUICK_REFERENCE.md)
- [Anti-Pattern Detection](docs/testing/ANTI_PATTERN_DETECTION.md)
- [Integration/E2E No-Mocks Policy](docs/testing/INTEGRATION_E2E_NO_MOCKS_POLICY.md)
- [TESTING_GUIDELINES.md](docs/development/business-requirements/TESTING_GUIDELINES.md)
