apiVersion: v1
kind: Secret
metadata:
  name: data-storage-secret
  namespace: kubernaut
type: Opaque
stringData:
  # Database Credentials (base64 encoded in production)
  DB_USER: "slm_user"
  DB_PASSWORD: "slm_password"

  # Embedding API Key (optional, base64 encoded in production)
  EMBEDDING_API_KEY: ""

  # Cache Password (optional, base64 encoded in production)
  CACHE_PASSWORD: ""

