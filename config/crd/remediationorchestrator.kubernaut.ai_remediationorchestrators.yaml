---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: remediationorchestrators.remediationorchestrator.kubernaut.ai
spec:
  group: remediationorchestrator.kubernaut.ai
  names:
    kind: RemediationOrchestrator
    listKind: RemediationOrchestratorList
    plural: remediationorchestrators
    singular: remediationorchestrator
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RemediationOrchestrator is the Schema for the remediationorchestrators
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RemediationOrchestratorSpec defines the desired state of
              RemediationOrchestrator.
            properties:
              foo:
                description: Foo is an example field of RemediationOrchestrator. Edit
                  remediationorchestrator_types.go to remove/update
                type: string
            type: object
          status:
            description: RemediationOrchestratorStatus defines the observed state
              of RemediationOrchestrator.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
