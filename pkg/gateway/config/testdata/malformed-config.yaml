# Malformed YAML file for testing error handling

server:
  listen_addr: ":8080"
  read_timeout: "30s"
  # Missing closing quote
  write_timeout: "30s
  idle_timeout: "120s"

# Invalid indentation
middleware:
  # Rate limiting removed (ADR-048)


