---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: remediationworkflows.kubernaut.ai
spec:
  group: kubernaut.ai
  names:
    kind: RemediationWorkflow
    listKind: RemediationWorkflowList
    plural: remediationworkflows
    shortNames:
    - rw
    singular: remediationworkflow
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.actionType
      name: Action
      type: string
    - jsonPath: .spec.execution.engine
      name: Engine
      type: string
    - jsonPath: .spec.metadata.version
      name: Version
      type: string
    - jsonPath: .status.catalogStatus
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          RemediationWorkflow is the Schema for the remediationworkflows API.
          BR-WORKFLOW-006: Kubernetes-native workflow schema definition.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              RemediationWorkflowSpec defines the desired state of RemediationWorkflow.
              Maps to the spec content of a workflow-schema.yaml file per BR-WORKFLOW-004.
            properties:
              actionType:
                description: ActionType is the action type from the taxonomy (PascalCase).
                type: string
              customLabels:
                additionalProperties:
                  type: string
                description: CustomLabels contains operator-defined key-value labels
                  for additional filtering
                type: object
              dependencies:
                description: Dependencies declares infrastructure resources required
                  by the workflow
                properties:
                  configMaps:
                    items:
                      description: RemediationWorkflowResourceDependency identifies
                        a Kubernetes resource by name
                      properties:
                        name:
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  secrets:
                    items:
                      description: RemediationWorkflowResourceDependency identifies
                        a Kubernetes resource by name
                      properties:
                        name:
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                type: object
              detectedLabels:
                description: DetectedLabels contains author-declared infrastructure
                  requirements
                x-kubernetes-preserve-unknown-fields: true
              execution:
                description: Execution contains execution engine configuration
                properties:
                  bundle:
                    description: Bundle is the execution bundle or container image
                      reference
                    type: string
                  bundleDigest:
                    description: BundleDigest is the digest of the execution bundle
                    type: string
                  engine:
                    description: Engine is the execution engine type
                    enum:
                    - tekton
                    - job
                    - ansible
                    type: string
                  engineConfig:
                    description: EngineConfig holds engine-specific configuration
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              labels:
                description: Labels contains mandatory matching/filtering criteria
                  for discovery
                properties:
                  component:
                    description: Component is the Kubernetes resource type
                    type: string
                  environment:
                    description: Environment is the target environment(s)
                    items:
                      type: string
                    minItems: 1
                    type: array
                  priority:
                    description: Priority is the business priority level
                    type: string
                  severity:
                    description: Severity is the severity level(s)
                    items:
                      type: string
                    minItems: 1
                    type: array
                required:
                - component
                - environment
                - priority
                - severity
                type: object
              metadata:
                description: Metadata contains workflow identification and description
                properties:
                  description:
                    description: Description is a structured description for LLM and
                      operator consumption
                    properties:
                      preconditions:
                        description: Preconditions describes conditions that must
                          be verified through investigation
                        type: string
                      what:
                        description: What describes what this workflow concretely
                          does
                        type: string
                      whenNotToUse:
                        description: WhenNotToUse describes specific exclusion conditions
                        type: string
                      whenToUse:
                        description: WhenToUse describes conditions under which this
                          workflow is appropriate
                        type: string
                    required:
                    - what
                    - whenToUse
                    type: object
                  maintainers:
                    description: Maintainers is optional maintainer information
                    items:
                      description: RemediationWorkflowMaintainer contains maintainer
                        contact information
                      properties:
                        email:
                          type: string
                        name:
                          type: string
                      required:
                      - email
                      - name
                      type: object
                    type: array
                  version:
                    description: Version is the semantic version (e.g., "1.0.0")
                    maxLength: 50
                    type: string
                  workflowId:
                    description: WorkflowID is the unique workflow identifier
                    maxLength: 255
                    type: string
                required:
                - description
                - version
                - workflowId
                type: object
              parameters:
                description: Parameters defines the workflow input parameters
                items:
                  description: RemediationWorkflowParameter defines a workflow input
                    parameter
                  properties:
                    default:
                      x-kubernetes-preserve-unknown-fields: true
                    dependsOn:
                      items:
                        type: string
                      type: array
                    description:
                      type: string
                    enum:
                      items:
                        type: string
                      type: array
                    maximum:
                      type: number
                    minimum:
                      type: number
                    name:
                      type: string
                    pattern:
                      type: string
                    required:
                      type: boolean
                    type:
                      enum:
                      - string
                      - integer
                      - boolean
                      - array
                      - float
                      type: string
                  required:
                  - description
                  - name
                  - required
                  - type
                  type: object
                minItems: 1
                type: array
              rollbackParameters:
                description: RollbackParameters defines parameters needed for rollback
                items:
                  description: RemediationWorkflowParameter defines a workflow input
                    parameter
                  properties:
                    default:
                      x-kubernetes-preserve-unknown-fields: true
                    dependsOn:
                      items:
                        type: string
                      type: array
                    description:
                      type: string
                    enum:
                      items:
                        type: string
                      type: array
                    maximum:
                      type: number
                    minimum:
                      type: number
                    name:
                      type: string
                    pattern:
                      type: string
                    required:
                      type: boolean
                    type:
                      enum:
                      - string
                      - integer
                      - boolean
                      - array
                      - float
                      type: string
                  required:
                  - description
                  - name
                  - required
                  - type
                  type: object
                type: array
            required:
            - actionType
            - execution
            - labels
            - metadata
            - parameters
            type: object
          status:
            description: RemediationWorkflowStatus defines the observed state of RemediationWorkflow
            properties:
              catalogStatus:
                description: CatalogStatus reflects the DS catalog state (active,
                  disabled, deprecated, archived)
                type: string
              previouslyExisted:
                description: PreviouslyExisted indicates if this workflow was re-registered
                  after deletion
                type: boolean
              registeredAt:
                description: RegisteredAt is the timestamp of initial registration
                format: date-time
                type: string
              registeredBy:
                description: RegisteredBy is the identity of the registrant
                type: string
              workflowId:
                description: WorkflowID is the UUID assigned by Data Storage upon
                  registration
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
